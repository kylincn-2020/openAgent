Metadata-Version: 2.4
Name: devops-agent
Version: 1.0.0
Summary: 让 DevOps 像对话一样简单 - 对话驱动的 DevOps 自动化 CLI 工具
Author: DevOps Agent Team
License: MIT
Project-URL: Homepage, https://github.com/your-org/devops-agent
Project-URL: Documentation, https://github.com/your-org/devops-agent/blob/main/docs/ARCHITECTURE.md
Project-URL: Repository, https://github.com/your-org/devops-agent
Project-URL: Issues, https://github.com/your-org/devops-agent/issues
Project-URL: Changelog, https://github.com/your-org/devops-agent/blob/main/CHANGELOG.md
Keywords: devops,ci-cd,github-actions,agent,cli,automation
Classifier: Development Status :: 5 - Production/Stable
Classifier: Intended Audience :: Developers
Classifier: License :: OSI Approved :: MIT License
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Classifier: Programming Language :: Python :: 3.13
Classifier: Topic :: Software Development :: Build Tools
Classifier: Topic :: Software Development :: Libraries :: Python Modules
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: typer[all]>=0.12
Requires-Dist: rich>=13.7
Requires-Dist: langgraph>=0.2
Requires-Dist: litellm>=1.40
Requires-Dist: fastapi>=0.110
Requires-Dist: pydantic>=2.0
Requires-Dist: pyyaml>=6.0
Requires-Dist: python-dotenv>=1.0
Requires-Dist: httpx>=0.27
Requires-Dist: uvicorn>=0.28
Requires-Dist: tenacity>=9.0
Requires-Dist: loguru>=0.7
Provides-Extra: dev
Requires-Dist: pytest>=7.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.21; extra == "dev"
Requires-Dist: pytest-cov>=4.0; extra == "dev"
Requires-Dist: ruff>=0.1; extra == "dev"
Requires-Dist: black>=23.0; extra == "dev"
Requires-Dist: isort>=5.12; extra == "dev"
Requires-Dist: mypy>=1.0; extra == "dev"

# DevOps Agent

**让 DevOps 像对话一样简单**

DevOps Agent 是一个对话驱动的 DevOps 自动化 CLI 工具。通过自然语言交互，自动分析项目并生成 CI/CD 配置，无需手动编写复杂的 YAML 文件。

## 特性

- **对话式交互**：用自然语言描述需求，无需记忆命令
- **智能项目分析**：自动识别项目类型、编程语言、依赖关系
- **CI/CD 配置生成**：自动生成 GitHub Actions 工作流配置
- **多语言支持**：Python、Node.js、Go、Java
- **可扩展架构**：轻松添加新 Agent 和语言支持

## 快速开始

### 安装

使用 pip 安装：

```bash
pip install devops-agent
```

或从源码安装：

```bash
git clone https://github.com/your-org/devops-agent.git
cd devops-agent
pip install -e .
```

### 配置

创建配置文件：

```bash
mkdir -p ~/config/devops-agent
cat > ~/config/devops-agent/config.yaml << EOF
llm:
  provider: claude
  api_key: your_api_key_here
  model: claude-3-5-sonnet-20241022
EOF
```

或设置环境变量：

```bash
export ANTHROPIC_API_KEY=your_api_key_here
```

### 使用

#### 对话模式

启动对话模式：

```bash
devops-agent start
```

然后自然输入你的需求：

```
You: 帮我分析这个项目

You: 生成 GitHub Actions 配置

You: 这个项目用什么框架？
```

#### 直接生成配置

跳过对话，直接生成 CI/CD 配置：

```bash
devops-agent build
```

#### 分析项目

分析当前项目：

```bash
devops-agent analyze
```

## 支持的语言

| 语言 | 检测文件 | 框架检测 |
|------|----------|----------|
| Python | `requirements.txt`, `pyproject.toml`, `setup.py` | Flask, Django, FastAPI |
| Node.js | `package.json` | React, Vue, Next.js, Express |
| Go | `go.mod` | - |
| Java | `pom.xml`, `build.gradle` | Spring Boot |

## 主要命令

| 命令 | 描述 |
|------|------|
| `devops-agent start` | 启动对话模式 |
| `devops-agent build` | 生成 CI/CD 配置 |
| `devops-agent analyze` | 分析项目 |

### 选项

- `--config, -c`：指定配置文件路径
- `--output, -o`：指定输出文件路径
- `--verbose`：显示详细日志

## 配置说明

### 配置文件位置

默认位置：`~/config/devops-agent/config.yaml`

可通过环境变量自定义：

```bash
export DEVOPS_AGENT_CONFIG=/path/to/config.yaml
```

### 配置项

```yaml
llm:
  provider: claude  # 或 openai
  api_key: your_api_key
  model: claude-3-5-sonnet-20241022
  timeout: 30

project:
  auto_detect: true
  max_depth: 100

output:
  format: yaml  # 或 json
  indent: 2
```

## 生成的配置示例

### Python 项目

```yaml
name: CI for Python

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest
```

### Node.js 项目

```yaml
name: CI for Node.js

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build
```

## 文档

- [架构文档](docs/ARCHITECTURE.md) - 系统架构和设计决策
- [扩展指南](docs/EXTENSIBILITY.md) - 如何添加新 Agent 和语言
- [贡献指南](docs/CONTRIBUTING.md) - 如何贡献代码

## 开发

### 设置开发环境

```bash
# 克隆仓库
git clone https://github.com/your-org/devops-agent.git
cd devops-agent

# 创建虚拟环境
python -m venv venv
source venv/bin/activate

# 安装开发依赖
pip install -e .[dev]

# 运行测试
pytest
```

### 代码风格

```bash
# 格式化代码
black .

# 排序导入
isort .

# 代码检查
ruff check src/
```

## 路线图

### v1.0（当前）

- [x] 基础 CLI 框架
- [x] 项目分析器
- [x] CI/CD 配置生成
- [x] 对话模式
- [x] 多语言支持（Python, Node.js, Go, Java）

### v1.1（计划中）

- 更多语言支持（Rust, Ruby, PHP）
- 配置模板优化
- 性能优化

### v2.0（未来）

- 持久化会话历史
- Web UI
- 更多 DevOps 工具支持（Docker, Kubernetes）
- 团队协作功能

## 常见问题

### Q: 如何配置 LLM API Key？

A: 有三种方式：

1. 配置文件：`~/config/devops-agent/config.yaml`
2. 环境变量：`export ANTHROPIC_API_KEY=xxx`
3. 运行时输入（对话模式）

### Q: 支持哪些 CI/CD 平台？

A: 目前只支持 GitHub Actions。计划在未来版本中支持更多平台。

### Q: 如何自定义配置模板？

A: 创建 `~/.devops-agent/templates/{language}/ci.yml.template`，系统会自动使用你的模板。

### Q: 项目分析失败怎么办？

A: 确保项目根目录有 `.git` 目录，并且包含支持的标记文件（如 `package.json`、`pyproject.toml`）。

### Q: 生成的配置可以直接使用吗？

A: 可以，但建议根据项目具体需求进行调整。系统会在生成前显示配置预览，你可以确认后写入。

## 许可证

[MIT License](LICENSE)

## 贡献

欢迎贡献！请阅读 [贡献指南](docs/CONTRIBUTING.md) 了解详情。

## 致谢

- [LangGraph](https://github.com/langchain-ai/langgraph) - 多 Agent 工作流编排
- [Typer](https://typer.tiangolo.com/) - CLI 框架
- [Rich](https://rich.readthedocs.io/) - 终端美化
- [LiteLLM](https://github.com/BerriAI/litellm) - 统一 LLM 接口

## 联系我们

- GitHub Issues: [报告问题](https://github.com/your-org/devops-agent/issues)
- Discussions: [提问和讨论](https://github.com/your-org/devops-agent/discussions)

---

**DevOps Agent** - 让 DevOps 像对话一样简单
